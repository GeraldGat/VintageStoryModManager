<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="PopupWindowStyle" TargetType="Window">
        <Setter Property="MinWidth" Value="300"/>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="ResizeMode" Value="CanResize"/>
        <Setter Property="AllowsTransparency" Value="True"/>
        <Setter Property="Background" Value="{DynamicResource WindowBackgroundColor}"/>
        <Setter Property="ShowInTaskbar" Value="False"/>
    </Style>

    <Style x:Key="PopupWindowBorderStyle" TargetType="Border">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WindowBorderColor}"/>
        <Setter Property="Padding" Value="20"/>
    </Style>

    <Style x:Key="PopupWindowBorderStyleWithActionBar" TargetType="Border" BasedOn="{StaticResource PopupWindowBorderStyle}">
        <Setter Property="Padding" Value="0"/>
    </Style>
    
</ResourceDictionary>