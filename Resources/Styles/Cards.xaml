<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="Card" TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}"/>
        <Style.Resources>
            <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="FontWeight" Value="Light"/>
            </Style>
        </Style.Resources>
    </Style>
    
    <Style x:Key="CardImage" TargetType="Border">
        <Setter Property="CornerRadius" Value="10,10,0,0"/>
    </Style>

    <Style x:Key="CardText" TargetType="Border">
        <Setter Property="Padding" Value="10,10"/>
    </Style>

    <Style x:Key="CardTitle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="0,0,0,2"/>
    </Style>

    <Style x:Key="FirstCardButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Border.CornerRadius" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="LastCardButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Border.CornerRadius" Value="0,0,10,0"/>
    </Style>

</ResourceDictionary>